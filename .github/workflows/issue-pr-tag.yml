
name: "Issue/PR Tag"

permissions:
  contents: read

# Trigger workflow when issues or pull requests are opened/edited,
# or when this workflow file is modified
on:
  issues:
    types: [opened, edited]
  pull_request:
    types: [opened, edited]

jobs:
  notify:
    uses: devops-actions/.github/.github/workflows/issue-pr-tag.yml@891fb6d59e53d81ffc7e9367de68d4ef288f2900 # main
    permissions:
      contents: read
      issues: write
      pull-requests: write
    with:
      org_name: ${{ vars.FIXED_ORG_NAME }}
      tag_team: ${{ vars.TAG_TEAM }}
      issue: ${{ github.event.issue.number }}
      pr: ${{ github.event.pull_request.number }}
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
