name: Validate Action Examples

on: [push, pull_request, workflow_dispatch]

permissions:
  contents: read

jobs:
  validate-examples:
    uses: devops-actions/.github/.github/workflows/actions-example-checker.yml@891fb6d59e53d81ffc7e9367de68d4ef288f2900 # main
    permissions:
      contents: read