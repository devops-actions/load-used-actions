name: Scorecards supply-chain security scan
on:
  # Only the default branch is supported.
  branch_protection_rule:
  schedule:
    # Weekly on Saturdays.
    - cron: '20 2 * * 6'
  push:
    branches: main
  workflow_dispatch:
    
# Declare default GITHUB_TOKEN permissions as read only.
permissions:
  contents: read

jobs:
  analysis:
    uses: devops-actions/.github/.github/workflows/rw-ossf-scorecard.yml@b77cea6d7ba1cd4e001581783cc592bbb63ce46d # main
    permissions:
      # Needed to upload the results to code-scanning dashboard.
      security-events: write
      id-token: write
      actions: read
      contents: read
